apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sentinel-gateway.fullname" . }}-config
  labels:
    {{- include "sentinel-gateway.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
